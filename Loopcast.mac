#turbo
Sub Main(gemNumber)

	/if (${Me.Sitting}) {
	  /stand
	}
	
	:Loop
	/if (!${Me.SpellReady[${gemNumber}]}) {
		/delay 1s
		/goto :Loop
	} else {
		/cast ${gemNumber}
		/delay 10s !${Me.Casting.ID}
		/goto :Loop
	}

/return